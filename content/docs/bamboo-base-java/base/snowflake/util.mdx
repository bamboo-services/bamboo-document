---
title: SnowflakeUtil
description: 标准雪花 ID 的静态工具类，提供生成、解析、校验与配置读取能力
icon: Wrench
---

import { TypeTable } from '@/components/type-table';

# SnowflakeUtil

`SnowflakeUtil` 位于 `com.xlf.utility.utility`，内部持有单例 `SnowflakeIdGenerator`。  
它是业务层最常用的标准雪花 ID 入口。

## 初始化机制

- Spring 环境：由 `SnowflakeUtilInitializer` 调用 `initialize(...)` 完成初始化。  
- 非 Spring 环境：首次调用时按默认值懒初始化（`datacenterId=1`、`machineId=1`、`epoch=1729440000000`）。

## 方法列表

### 生成方法

<TypeTable
  type={{
    generateId: {
      description: '生成标准雪花 ID（long）。',
      type: '() long',
      returns: '64 位长整型 ID',
    },
    generateIdString: {
      description: '生成字符串形式的雪花 ID。',
      type: '() String',
      returns: '数字字符串',
    },
  }}
/>

### 解析方法

<TypeTable
  type={{
    parseTimestamp: {
      description: '解析真实时间戳（毫秒，epoch + 偏移量）。',
      type: '(long id) long',
      returns: 'Unix 毫秒时间戳',
    },
    parseDatacenterId: {
      description: '解析数据中心 ID。',
      type: '(long id) long',
      returns: '范围 0~31',
    },
    parseMachineId: {
      description: '解析机器 ID。',
      type: '(long id) long',
      returns: '范围 0~31',
    },
    parseSequence: {
      description: '解析序列号。',
      type: '(long id) long',
      returns: '范围 0~4095',
    },
    parseInfo: {
      description: '解析为 SnowflakeInfoDTO。注意 DTO 的 timestamp 字段为相对 epoch 的偏移值。',
      type: '(long id) SnowflakeInfoDTO',
      returns: '完整结构化信息',
    },
  }}
/>

### 校验与配置

<TypeTable
  type={{
    isValid: {
      description: '校验 ID 合法性（参数类型为 Long，可传 null）。',
      type: '(Long id) boolean',
      returns: '合法返回 true',
    },
    getConfigInfo: {
      description: '返回当前 datacenterId、machineId、epoch 的可读字符串。',
      type: '() String',
      returns: '配置摘要字符串',
    },
    getDatacenterId: {
      description: '获取当前数据中心 ID。',
      type: '() long',
      returns: '0~31',
    },
    getMachineId: {
      description: '获取当前机器 ID。',
      type: '() long',
      returns: '0~31',
    },
    getEpoch: {
      description: '获取当前 epoch（毫秒）。',
      type: '() long',
      returns: 'epoch',
    },
    initialize: {
      description: '注入外部创建的 SnowflakeIdGenerator（通常由框架启动阶段调用一次）。',
      type: '(SnowflakeIdGenerator generator) void',
    },
  }}
/>

## 使用示例

### 生成与解析

```java title="Example.java"
import com.xlf.utility.models.dto.SnowflakeInfoDTO;
import com.xlf.utility.utility.SnowflakeUtil;

long id = SnowflakeUtil.generateId();
String idString = SnowflakeUtil.generateIdString();

long realTimestamp = SnowflakeUtil.parseTimestamp(id);
SnowflakeInfoDTO info = SnowflakeUtil.parseInfo(id);

System.out.println(idString);
System.out.println(realTimestamp);
System.out.println(info.getGeneratedTime());
```

### 合法性校验

```java title="Example.java"
Long id = SnowflakeUtil.generateId();
boolean ok = SnowflakeUtil.isValid(id);
boolean invalid = SnowflakeUtil.isValid(null);
```

### 非 Spring 场景手动初始化

```java title="Example.java"
import com.xlf.utility.incrementer.SnowflakeIdGenerator;
import com.xlf.utility.utility.SnowflakeUtil;

SnowflakeIdGenerator generator = new SnowflakeIdGenerator(1, 1, 1690214400000L);
SnowflakeUtil.initialize(generator);
```

## 下一步

<Cards>
  <Card title="SnowflakeIdGenerator" href="/docs/bamboo-base-java/base/snowflake/generator" />
  <Card title="SnowflakeInfoDTO" href="/docs/bamboo-base-java/base/snowflake/info-dto" />
  <Card title="GeneSnowflakeUtil" href="/docs/bamboo-base-java/base/snowflake/gene-util" />
</Cards>
