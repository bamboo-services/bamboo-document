---
title: PermissionWebFilter
description: 抽象权限过滤器，基于 @NeedPermission 注解实现接口权限校验
icon: Lock
---

# PermissionWebFilter

`PermissionWebFilter` 是 `bamboo-webflux` 模块提供的抽象权限过滤器，通过检查 Controller 方法上的 `@NeedPermission` 注解来决定是否执行权限校验。开发者需继承该抽象类并实现具体的权限校验逻辑。

## 类定义

```java title="PermissionWebFilter.java"
// [!code highlight:3]
// 抽象权限过滤器，需要开发者实现具体的权限校验逻辑
public abstract class PermissionWebFilter implements WebFilter {

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, WebFilterChain chain) {
        // ...
    }

    // [!code highlight:3]
    // 抽象方法，由子类实现具体的权限校验逻辑
    protected abstract Mono<Void> hasPermissionCheck(
            ServerWebExchange exchange, String permission);
}
```

## 工作原理

1. 拦截请求，通过 `RequestMappingHandlerMapping` 解析出目标 Handler Method
2. 检查该方法是否标注了 `@NeedPermission` 注解
3. 若存在注解，提取权限标识，调用抽象方法 `hasPermissionCheck` 执行校验
4. 若不存在注解，直接放行请求

```java title="PermissionWebFilter.java"
@Override
public Mono<Void> filter(ServerWebExchange exchange, WebFilterChain chain) {
    // [!code highlight:6]
    // 获取目标 HandlerMethod 并检查 @NeedPermission 注解
    return ServerWebExchangeUtil.getHandlerMethod(exchange, handlerMapping)
            .flatMap(handlerMethod -> {
                NeedPermission annotation = handlerMethod.getMethodAnnotation(NeedPermission.class);
                if (annotation != null) {
                    return this.hasPermissionCheck(exchange, annotation.value())
                            .then(chain.filter(exchange));
                }
                return chain.filter(exchange);
            })
            .switchIfEmpty(chain.filter(exchange));
}
```

## 实现示例

```java title="CustomPermissionFilter.java"
package com.example.demo.filter;

import com.xlf.utility.webflux.filter.PermissionWebFilter;
import org.springframework.http.HttpStatus;
import org.springframework.stereotype.Component;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Mono;

// [!code highlight:2]
// 继承 PermissionWebFilter 并实现 hasPermissionCheck 方法
@Component
public class CustomPermissionFilter extends PermissionWebFilter {

    private final PermissionService permissionService;

    public CustomPermissionFilter(PermissionService permissionService,
                                   RequestMappingHandlerMapping handlerMapping) {
        super(handlerMapping);
        this.permissionService = permissionService;
    }

    @Override
    // [!code highlight:3]
    // 实现具体的权限校验逻辑
    // 返回 Mono<Void>，校验通过则正常完成，失败则抛出异常
    protected Mono<Void> hasPermissionCheck(ServerWebExchange exchange, String permission) {
        String token = exchange.getRequest().getHeaders().getFirst("Authorization");
        return permissionService.checkPermission(token, permission)
                .flatMap(hasPermission -> {
                    if (!hasPermission) {
                        exchange.getResponse().setStatusCode(HttpStatus.FORBIDDEN);
                        return exchange.getResponse().setComplete();
                    }
                    return Mono.empty();
                });
    }
}
```

## 配合 @NeedPermission 注解使用

在 Controller 方法上标注 `@NeedPermission` 注解，声明该接口所需的权限：

```java title="UserController.java"
@RestController
@RequestMapping("/api/v1/user")
public class UserController {

    // [!code highlight:2]
    // 标注所需权限，PermissionWebFilter 会自动校验
    @NeedPermission("user:read")
    @GetMapping("/{id}")
    public Mono<ResponseEntity<BaseResponse<UserDTO>>> getUser(@PathVariable Long id) {
        return userService.getUserById(id)
                .flatMap(user -> ResultUtil.success("获取成功", user));
    }

    @NeedPermission("user:write")
    @PostMapping
    public Mono<ResponseEntity<BaseResponse<Void>>> createUser(@RequestBody UserDTO dto) {
        return userService.createUser(dto)
                .then(ResultUtil.success("创建成功"));
    }
}
```

## 与 MVC 版本的区别

| 特性 | MVC PermissionFilter | WebFlux PermissionWebFilter |
|------|---------------------|---------------------------|
| 接口 | `javax.servlet.Filter` | `WebFilter` |
| 抽象方法 | `hasPermissionCheck(HttpServletRequest, String)` | `hasPermissionCheck(ServerWebExchange, String) -> Mono<Void>` |
| 返回类型 | `void`（抛异常表示失败） | `Mono<Void>`（响应式信号） |
| Handler 解析 | `HandlerMapping.getHandler()` | `ServerWebExchangeUtil.getHandlerMethod()` |

## 注意事项

1. **该过滤器不由自动配置注册** - 开发者需自行将实现类注册为 Spring Bean。
2. **确保注入 `RequestMappingHandlerMapping`** - 权限过滤器需要通过它解析目标 Handler Method。
3. **校验失败处理** - 在 `hasPermissionCheck` 中，校验失败时应抛出异常（如 `UserAuthenticationException`）或直接设置响应状态码。

## 下一步

<Cards>
  <Card title="ContextFilter" href="/docs/bamboo-base-java/webflux/filter/context" />
  <Card title="CORS 过滤器" href="/docs/bamboo-base-java/webflux/filter/cors" />
  <Card title="WebFilter 概览" href="/docs/bamboo-base-java/webflux/filter" />
</Cards>
