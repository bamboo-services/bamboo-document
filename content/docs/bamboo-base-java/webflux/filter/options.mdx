---
title: AllowOptionWebFilter
description: 响应式 OPTIONS 预检请求处理过滤器
icon: CheckCircle
---

# AllowOptionWebFilter

`AllowOptionWebFilter` 是 `bamboo-webflux` 模块的 OPTIONS 预检请求处理过滤器，用于拦截浏览器发出的 CORS 预检请求（HTTP OPTIONS 方法），直接返回 `200 OK` 响应。

## 类定义

```java title="AllowOptionWebFilter.java"
// [!code highlight:4]
// OPTIONS 预检过滤器，优先级略低于 CORS 过滤器
@Order(Ordered.HIGHEST_PRECEDENCE + 1)
public class AllowOptionWebFilter implements WebFilter {

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, WebFilterChain chain) {
        // ...
    }
}
```

## 工作原理

当浏览器执行跨域请求时，对于非简单请求会先发送一个 OPTIONS 预检请求。`AllowOptionWebFilter` 在过滤器链的早期阶段拦截这类请求，避免其进入 Controller 层的业务逻辑。

```java title="AllowOptionWebFilter.java"
@Override
public Mono<Void> filter(ServerWebExchange exchange, WebFilterChain chain) {
    // [!code highlight:5]
    // 判断请求方法是否为 OPTIONS
    if (exchange.getRequest().getMethod() == HttpMethod.OPTIONS) {
        exchange.getResponse().setStatusCode(HttpStatus.OK);
        return exchange.getResponse().setComplete();
    }

    // 非 OPTIONS 请求，继续传递至下一个过滤器
    return chain.filter(exchange);
}
```

## 执行流程

```
浏览器跨域请求
  │
  ├── 预检请求 (OPTIONS /api/v1/user)
  │     │
  │     ├── AllowCorsWebFilter → 设置 CORS 头
  │     │
  │     └── AllowOptionWebFilter → 返回 200 OK（终止链路）
  │
  └── 实际请求 (POST /api/v1/user)
        │
        ├── AllowCorsWebFilter → 设置 CORS 头
        │
        ├── AllowOptionWebFilter → 非 OPTIONS，跳过
        │
        └── 继续执行后续过滤器 → Controller
```

## Order 说明

该过滤器的 Order 为 `HIGHEST_PRECEDENCE + 1`，比 `AllowCorsWebFilter`（`HIGHEST_PRECEDENCE`）低一级。这确保了 CORS 响应头在 OPTIONS 请求返回之前已经被设置。

## 下一步

<Cards>
  <Card title="CORS 过滤器" href="/docs/bamboo-base-java/webflux/filter/cors" />
  <Card title="ContextFilter" href="/docs/bamboo-base-java/webflux/filter/context" />
  <Card title="WebFilter 概览" href="/docs/bamboo-base-java/webflux/filter" />
</Cards>
