---
title: DebugAspectHandler
description: 响应式调试切面，控制调试接口的访问权限
icon: Bug
---

# DebugAspectHandler

`DebugAspectHandler` 是 `bamboo-webflux` 模块的调试切面处理器，用于控制调试类接口的访问权限。在生产环境中，通过该切面可以限制或禁用调试接口的调用。

## 类定义

```java title="DebugAspectHandler.java"
// [!code highlight:3]
// 调试切面，由自动配置注册
@Aspect
public class DebugAspectHandler {
    // ...
}
```

## 核心方法

### checkDebugController

同步方式检查调试接口的访问权限。适用于同步方法的调试控制。

```java title="DebugAspectHandler.java"
// [!code highlight:2]
// 同步检查调试接口权限
public void checkDebugController() {
    // 检查当前环境是否允许调试接口访问
}
```

### checkDebugControllerReactive

响应式方式检查调试接口的访问权限。通过 `ServerWebExchange` 获取请求上下文信息。

```java title="DebugAspectHandler.java"
// [!code highlight:3]
// 响应式检查调试接口权限
// 接受 ServerWebExchange 参数以获取请求信息
public void checkDebugControllerReactive(ServerWebExchange exchange) {
    // 从 exchange 中获取请求信息进行权限校验
}
```

### debugCheckTransformer

提供响应式流转换器，用于在 `Mono` / `Flux` 管道中插入调试权限检查逻辑。

```java title="DebugAspectHandler.java"
// [!code highlight:3]
// 返回响应式转换器，可嵌入 Mono/Flux 管道
public <T> Function<Mono<T>, Mono<T>> debugCheckTransformer() {
    return mono -> mono.transformDeferredContextual((m, ctx) -> {
        // 在响应式管道中执行调试权限检查
        return m;
    });
}
```

**使用示例：**

```java title="DebugController.java"
@RestController
@RequestMapping("/debug")
public class DebugController {

    private final DebugAspectHandler debugAspect;

    @GetMapping("/info")
    public Mono<ResponseEntity<BaseResponse<DebugInfo>>> getDebugInfo() {
        // [!code highlight:3]
        // 在响应式管道中使用 debugCheckTransformer 进行权限检查
        return Mono.just(buildDebugInfo())
                .transform(debugAspect.debugCheckTransformer())
                .flatMap(info -> ResultUtil.success("调试信息", info));
    }
}
```

## 与 MVC 版本的差异

| 特性 | MVC 版本 | WebFlux 版本 |
|------|---------|-------------|
| 请求对象 | `HttpServletRequest` | `ServerWebExchange` |
| 权限检查方式 | 仅同步 | 同步 + 响应式 |
| 管道集成 | 不适用 | `debugCheckTransformer()` |

WebFlux 版本新增了 `debugCheckTransformer()` 方法，使得调试权限检查可以自然地融入 Reactor 管道，无需在切面层面处理响应式类型。

## 自动注册

`DebugAspectHandler` 由 `WebFluxSdkAutoConfiguration` 自动注册：

```java title="WebFluxSdkAutoConfiguration.java"
@Bean
public DebugAspectHandler debugAspectHandler() {
    return new DebugAspectHandler();
}
```

## 下一步

<Cards>
  <Card title="日志切面" href="/docs/bamboo-base-java/webflux/aspect/log" />
  <Card title="WebFilter 概览" href="/docs/bamboo-base-java/webflux/filter" />
  <Card title="自动配置" href="/docs/bamboo-base-java/webflux/auto-configuration" />
</Cards>
