---
title: 异常处理概览
description: bamboo-mvc 分层异常处理架构，提供从 Java 基础异常到业务异常的全面覆盖
icon: ShieldAlert
---

# 异常处理概览

`bamboo-mvc` 提供分层式异常处理架构，通过继承链实现从底层 Java 异常到上层业务异常的全面捕获。所有异常处理器均标注 `@RestControllerAdvice`，返回符合 `BaseResponse<T>` 规范的标准化响应。

<Callout type="info" title="公共异常处理文档">
Java 基础异常、MySQL/PostgreSQL 异常、业务异常等内容已归纳到 [核心库异常处理文档](/docs/bamboo-base-java/base/exception)，MVC 和 WebFlux 共用。
</Callout>

## 继承层级

```
JavaBaseExceptionHandler          ← Java 基础异常（IOException, NullPointerException 等）
    └── PublicExceptionHandler    ← 公共业务异常（BusinessException, CheckFailureException 等）
        └── SystemExceptionHandler ← Spring MVC 异常（MethodArgumentNotValidException 等）
```

每一层处理器继承上一层的全部异常处理能力，并在此基础上扩展新的异常类型。项目中只需继承最顶层的 `SystemExceptionHandler` 即可获得完整的异常处理能力。

## MVC 特有处理器

| 处理器 | 职责 | 说明 |
|--------|------|------|
| `SystemExceptionHandler` | Spring MVC 框架异常 | 仅 MVC，处理 `HttpRequestMethodNotSupportedException` 等 |
| `UserAuthExceptionHandler` | 用户认证异常 | 仅 MVC，处理 `UserAuthenticationException` |

## 扩展方式

在项目中创建自定义异常处理器，继承 `SystemExceptionHandler` 即可获得全部内置异常处理能力：

```java title="GlobalExceptionHandler.java"
// [!code highlight:2]
// 继承 SystemExceptionHandler 获得完整的异常处理链
@RestControllerAdvice
public class GlobalExceptionHandler extends SystemExceptionHandler {

    // [!code highlight:2]
    // 添加项目特有的异常处理
    @ExceptionHandler(CustomBusinessException.class)
    public ResponseEntity<BaseResponse<Void>> handleCustom(CustomBusinessException e) {
        return ResultUtil.error(ErrorCode.OPERATION_FAILED, e.getMessage(), null);
    }
}
```

## 下一步

<Cards>
  <Card title="异常处理架构（公共）" href="/docs/bamboo-base-java/base/exception" />
  <Card title="Spring MVC 异常（仅 MVC）" href="/docs/bamboo-base-java/mvc/exception/spring-boot" />
  <Card title="用户认证异常（仅 MVC）" href="/docs/bamboo-base-java/mvc/exception/user-auth" />
  <Card title="自定义异常处理" href="/docs/bamboo-base-java/mvc/exception/custom-system" />
</Cards>
