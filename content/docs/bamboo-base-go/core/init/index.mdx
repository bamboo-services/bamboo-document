---
title: 快速开始
description: xReg 包提供节点化初始化系统，支持灵活的组件注册
icon: Rocket
---

import { TypeTable } from '@/components/type-table';

# xReg 包

`xReg` 包是 Bamboo Base 的核心注册模块，采用**节点化模式**实现灵活的组件初始化和依赖注入。

```go
import xReg "github.com/bamboo-services/bamboo-base-go/major/register"
import xRegNode "github.com/bamboo-services/bamboo-base-go/major/register/node"
```

## Reg 结构体

应用程序的核心注册结构，包含所有初始化后的组件实例：

```go title="register.go"
type Reg struct {
    // [!code highlight:2]
    Serve *gin.Engine       // Gin 引擎实例
    Init  *xRegNode.RegNode // 初始化节点管理器
}
```

<TypeTable
  type={{
    Serve: {
      description: 'Gin 引擎实例，用于处理 HTTP 请求',
      type: '*gin.Engine',
      required: true,
    },
    Init: {
      description: '节点管理器，包含初始化上下文和节点列表',
      type: '*xRegNode.RegNode',
      required: true,
    },
  }}
/>

## Register

注册并初始化应用的所有核心组件，支持自定义初始化节点。

```go title="register.go"
// [!code highlight]
func Register(ctx context.Context, nodeList []xRegNode.RegNodeList, opts ...xOption.Option) *Reg
```

**参数说明：**

<TypeTable
  type={{
    ctx: {
      description: '上下文，用于控制取消和超时',
      type: 'context.Context',
      required: true,
    },
    nodeList: {
      description: '自定义初始化节点列表（不得包含框架保留键，见下方说明）',
      type: '[]xRegNode.RegNodeList',
      required: true,
    },
    opts: {
      description: 'v1.0.5 起新增。应用级配置选项（缓存、数据库、路由等），nil 或不传表示不启用内置实现',
      type: '...xOption.Option',
      required: false,
    },
  }}
/>

> **框架保留键**：`SnowflakeNodeKey` / `DatabaseKey` / `CacheManagerKey` / `RedisClientKey` 由 `Register` 内部根据 `opts` 自动注册，**不得**出现在 `nodeList` 中，否则 `Use` 会因重复注册 panic。

**初始化顺序：**

```
Register(ctx, nodeList, opts...)
    ├── configInit()           // 1. 加载 .env 配置（私有）
    ├── loggerInit()           // 2. 初始化日志（私有）
    ├── SnowflakeInit          // 3. 基础设施：雪花算法节点（框架注册）
    ├── DatabaseInit           // 4. 基础设施：数据库（来自 opts，启用时）
    ├── CacheInit              // 5. 基础设施：缓存（来自 opts，启用时）
    │   └── RedisClientFromManager  // 5.1 Redis 后端补注册 *redis.Client
    ├── [自定义节点...]         // 6. 业务自定义节点（来自 nodeList）
    ├── Init.Exec()            // 7. 一次性执行全部装配
    ├── engineInit()           // 8. 初始化 Gin 引擎（私有）
    └── 路由注册                // 9. 按 opts.Routes() 顺序挂载路由

启动阶段（推荐）
    └── xMain.Runner(...)      // 10. 启动 HTTP 服务并托管优雅关闭
```

**示例：**

```go title="main.go"
package main

import (
    "context"

    xCtx "github.com/bamboo-services/bamboo-base-go/defined/context"
    xLog "github.com/bamboo-services/bamboo-base-go/common/log"
    xMain "github.com/bamboo-services/bamboo-base-go/major/main"
    xOption "github.com/bamboo-services/bamboo-base-go/major/option"
    xOptDatabase "github.com/bamboo-services/bamboo-base-go/major/option/database"
    xReg "github.com/bamboo-services/bamboo-base-go/major/register"
    xRegNode "github.com/bamboo-services/bamboo-base-go/major/register/node"
    "github.com/gin-gonic/gin"
)

func main() {
    // [!code highlight:10]
    // v1.0.5 起：opts 直接传给 Register，统一装配内置组件
    // 业务自定义节点列表只放纯业务逻辑，不再手写数据库初始化
    reg := xReg.Register(context.Background(),
        []xRegNode.RegNodeList{
            {Key: xCtx.CustomClientKey, Node: initCustomSDK},
        },
        xOption.WithDatabase(xOptDatabase.FromEnv()), // 从环境变量装配数据库
        xOption.WithRoute(func(ctx context.Context, serve *gin.Engine) {
            serve.GET("/ping", func(c *gin.Context) {
                c.String(http.StatusOK, "pong")
            })
        }),
    )

    log := xLog.WithName(xLog.NamedMAIN)
    xMain.Runner(reg, log, nil)
}

// [!code highlight:2]
// 自定义初始化节点（业务 SDK / 第三方组件）
func initCustomSDK(ctx context.Context) (any, error) {
    // 可直接从 ctx 取 *gorm.DB / *xCache.Manager（已由 opts 装配）
    db := ctx.Value(xCtx.DatabaseKey).(*gorm.DB)
    // ...
    return sdk, nil
}
```

## 节点化系统

### RegNode 结构

```go title="register/node/node.go"
// [!code highlight:2]
// Node 定义初始化函数签名
type Node func(ctx context.Context) (any, error)

// [!code highlight:2]
// RegNodeList 存储节点信息
type RegNodeList struct {
    Key  xCtx.ContextKey  // 上下文键
    Node Node                // 初始化函数
}

// [!code highlight:2]
// RegNode 节点管理器
type RegNode struct {
    list []RegNodeList
    Ctx  context.Context
}
```

### 核心方法

```go title="register/node/node.go"
// [!code highlight:2]
// Use 注册初始化节点
func (rn *RegNode) Use(ctxKey xCtx.ContextKey, registerFunc Node)

// [!code highlight:2]
// Exec 按顺序执行所有节点
func (rn *RegNode) Exec()
```

### 执行流程

```go title="register/node/node.go"
func (rn *RegNode) Exec() {
    for i, node := range rn.list {
        // [!code highlight:2]
        // 执行节点初始化
        val, err := node.Node(rn.Ctx)
        if err != nil {
            panic(fmt.Sprintf("执行注册节点失败: index=%d Key=%v err=%v", i, node.Key, err))
        }
        // [!code highlight:2]
        // 将结果存入上下文，供后续节点使用
        rn.Ctx = context.WithValue(rn.Ctx, node.Key, val)
    }
    rn.list = nil // 释放内存
}
```

**特点：**
- 🔄 **依赖注入**：每个节点可访问前面节点的初始化结果
- 📊 **顺序执行**：按注册顺序依次初始化
- 🛡️ **错误处理**：任何节点失败都会 panic 并输出详细信息

## 内置初始化

### 配置初始化（私有）

从 `.env` 文件加载环境变量配置。

详见：[配置初始化](/docs/bamboo-base-go/core/init/config)

### 日志初始化（私有）

初始化日志记录器，支持控制台彩色输出和文件 JSON 记录。

详见：[日志初始化](/docs/bamboo-base-go/core/init/logger)

### 雪花算法节点（内置）

作为内置节点自动注册，初始化雪花算法节点。

```go title="register.go"
// [!code highlight]
reg.Init.Use(xCtx.SnowflakeNodeKey, xInit.SnowflakeInit)
```

详见：[雪花算法初始化](/docs/bamboo-base-go/core/init/snowflake)

### 引擎初始化（私有）

初始化 Gin 引擎，注册内置中间件和验证器，并注入初始化上下文。

详见：[引擎初始化](/docs/bamboo-base-go/core/init/engine)

### 服务运行时（公共）

推荐使用 `xMain.Runner` 启动服务，统一处理：

- 信号监听（`SIGINT`/`SIGTERM`）
- 优雅关闭
- 后台协程托管

详见：[服务运行时](/docs/bamboo-base-go/core/init/runner)

> gRPC 为可选能力，已拆分到独立板块：[`gRPC（可选）`](/docs/bamboo-base-go/grpc)

## 上下文注入

引擎初始化时会自动将节点管理器的上下文注入到每个 HTTP 请求：

```go title="register_gin.go"
// [!code highlight:2]
// 注入初始化上下文到每个请求
func injectContext(ctx context.Context) func(c *gin.Context) {
    return func(c *gin.Context) {
        c.Request = c.Request.WithContext(ctx)
        c.Next()
    }
}
```

这意味着在业务代码中可以通过 `c.Request.Context()` 获取初始化时注入的所有资源。

## 调试模式

通过环境变量 `XLF_DEBUG` 控制调试模式：

| 环境变量 | 值 | 效果 |
|---------|-----|------|
| `XLF_DEBUG` | `true` | 启用调试模式，日志级别为 Debug |
| `XLF_DEBUG` | `false` 或未设置 | 生产模式，日志级别为 Info |

## 完整示例

```go title="main.go"
package main

import (
    "context"

    xCtx "github.com/bamboo-services/bamboo-base-go/defined/context"
    xOption "github.com/bamboo-services/bamboo-base-go/major/option"
    xOptCache "github.com/bamboo-services/bamboo-base-go/major/option/cache"
    xOptDatabase "github.com/bamboo-services/bamboo-base-go/major/option/database"
    xReg "github.com/bamboo-services/bamboo-base-go/major/register"
    xRegNode "github.com/bamboo-services/bamboo-base-go/major/register/node"
    xResult "github.com/bamboo-services/bamboo-base-go/major/result"
    "github.com/gin-gonic/gin"
)

func main() {
    // [!code highlight:9]
    // v1.0.5 起：业务节点只放纯业务逻辑，数据库/缓存交给 opts
    reg := xReg.Register(context.Background(),
        []xRegNode.RegNodeList{
            {Key: xCtx.CustomClientKey, Node: initCustomSDK},
        },
        xOption.WithDatabase(xOptDatabase.MySQL("user:pass@tcp(localhost:3306)/bamboo")),
        xOption.WithCache(xOptCache.WithRedis("localhost:6379")),
    )

    // 注册路由
    // [!code highlight:4]
    reg.Serve.GET("/api/status", func(c *gin.Context) {
        xResult.SuccessHasData(c, "服务正常", gin.H{
            "status": "running",
        })
    })

    // 启动服务
    // [!code highlight]
    reg.Serve.Run(":8080")
}

// [!code highlight:2]
// 业务自定义初始化节点（数据库/缓存已由框架装配，勿重复注册保留键）
func initCustomSDK(ctx context.Context) (any, error) {
    db := ctx.Value(xCtx.DatabaseKey).(*gorm.DB)
    // 基于 db 初始化自定义 SDK...
    return sdk, nil
}
```

## 下一步

<Cards>
  <Card title="服务运行时" href="/docs/bamboo-base-go/core/init/runner" />
  <Card title="配置初始化" href="/docs/bamboo-base-go/core/init/config" />
  <Card title="日志初始化" href="/docs/bamboo-base-go/core/init/logger" />
  <Card title="雪花算法初始化" href="/docs/bamboo-base-go/core/init/snowflake" />
  <Card title="引擎初始化" href="/docs/bamboo-base-go/core/init/engine" />
</Cards>
