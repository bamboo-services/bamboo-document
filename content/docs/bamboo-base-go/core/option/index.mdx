---
title: 声明式配置
description: xOption 函数式选项模式，声明式装配缓存、数据库和路由
icon: Settings
---

import { TypeTable } from '@/components/type-table';

# xOption — 声明式配置

`xOption` 基于**函数式选项模式**，让业务侧以声明式风格选择框架内置组件（缓存后端、数据库驱动、路由挂载），而非手工编写初始化节点。Register 在启动时根据 `Config` 自动完成装配。

```go
import (
    xOption     "github.com/bamboo-services/bamboo-base-go/major/option"
    xOptCache   "github.com/bamboo-services/bamboo-base-go/major/option/cache"
    xOptDatabase "github.com/bamboo-services/bamboo-base-go/major/option/database"
)
```

## 设计理念

传统的 `xRegNode.RegNodeList` 要求业务侧编写完整的初始化工厂函数（打开连接、拼装 DSN、注册中间件）。对于框架内置组件（缓存、数据库、路由），xOption 将这部分逻辑内化到框架层，业务侧只需描述「选哪种 + 用什么参数」。

v1.0.5 起，`Register` 与 `Runner` 均接受 `opts ...xOption.Option`，opts 既可作为变参传给 `Register`（业务自定义节点立即拿到 DB / 缓存），也可切片传给 `Runner`（仅启动阶段装配）。两者**不是二选一**，而是同一套 opts 的两种注入时机：

```
方式 A：opts 传给 Register（推荐，业务节点立即可用）
─────────────────────────────────────────────────────
reg := xReg.Register(ctx, nodeList,
    xOption.WithCache(xOptCache.WithRedis(...)),
    xOption.WithDatabase(xOptDatabase.MySQL(...)),
    xOption.WithRoute(routeFunc),
)
xMain.Runner(reg, log, nil)

方式 B：opts 传给 Runner（启动阶段才装配）
─────────────────────────────────────────────────────
reg := xReg.Register(ctx, nodeList)
opts := []xOption.Option{
    xOption.WithCache(xOptCache.WithRedis(...)),
    xOption.WithDatabase(xOptDatabase.MySQL(...)),
    xOption.WithRoute(routeFunc),
}
xMain.Runner(reg, log, opts)
```

## 两层调用形态

v1.0.4 起，**缓存**与**数据库**采用对称的「两层调用」设计，将类型/构造函数收敛到 `xOptCache` / `xOptDatabase` 子包，父包 `xOption` 仅保留 `WithCache` / `WithDatabase` 顶层入口与类型别名重导出：

```
xOption.WithCache(xOptCache.WithRedis("localhost:6379"))
└─ 父包 Option  └─ 子包 CacheOption（具体后端构造）
xOption.WithDatabase(xOptDatabase.MySQL("user:pass@tcp(...)/db"))
└─ 父包 Option  └─ 子包 DatabaseOption（具体驱动构造）
```

| 维度 | 父包（`xOption`） | 子包（`xOptCache` / `xOptDatabase`） |
|------|-------------------|--------------------------------------|
| 类型别名 | `CacheType` / `Driver` / `CacheConfig` / `DatabaseConfig` | 子包原定义 |
| 顶层入口 | `WithCache` / `WithDatabase` / `WithRoute` | — |
| 后端构造 | — | `WithRedis` / `WithMemory` / `MySQL` / `Oracle` / `SQLServer` / `FromEnv` 等 |
| 二级选项 | — | `WithRedisPassword` / `WithMaxOpenConns` / `WithAutoMigrate` 等 |

> 父包对常用类型做了**别名重导出**（如 `xOption.DriverMySQL`、`xOption.CacheTypeRedis`），保证旧引用 `xOption.DriverXXX` 仍可编译；但**构造函数**（`MySQL` / `WithRedis` / `FromEnv` 等）请直接走子包，避免父包冗余包装。

## 核心类型

### Option & Config

```go
// Option 是应用级配置选项的函数签名
type Option func(*Config)

// Config 是应用运行期配置的聚合体，字段均私有，仅通过 getter 暴露只读视图
type Config struct { /* 私有字段 */ }

func (c *Config) Cache()    CacheConfig           // 缓存配置
func (c *Config) Database() xOptDatabase.DatabaseConfig // 数据库配置
func (c *Config) Routes()   []RouteRegistrar     // 路由注册器列表
```

### Apply

将 Option 列表逐个应用到 Config，nil 选项自动跳过。

```go
func Apply(opts ...Option) *Config
```

## 缓存配置

<TypeTable
  type={{
    'WithCache': {
      description: '将子包 CacheOption 包裹为顶层 Option，供 Register 使用',
      type: 'func(opts ...xOptCache.CacheOption) Option',
      required: false,
    },
  }}
/>

### 后端构造（子包 `xOptCache`）

<TypeTable
  type={{
    'WithRedis': {
      description: '配置 Redis 作为缓存后端，addr 为 host:port',
      type: 'func(addr string, opts ...RedisOption) CacheOption',
      required: false,
    },
    'WithMemory': {
      description: '配置程序内内存作为缓存后端',
      type: 'func(opts ...MemoryOption) CacheOption',
      required: false,
    },
    'WithCacheType': {
      description: '显式指定缓存类型（通常无需手动调用，WithRedis/WithMemory 已隐式设置）',
      type: 'func(t CacheType) CacheOption',
      required: false,
    },
    'FromEnv': {
      description: '从环境变量自动装配（读取 NOSQL_DRIVER 等），未启用时返回 nil',
      type: 'func() CacheOption',
      required: false,
    },
  }}
/>

### Redis 子选项

| 选项 | 类型 | 说明 |
|------|------|------|
| `xOptCache.WithRedisUsername(u)` | `RedisOption` | ACL 用户名 |
| `xOptCache.WithRedisPassword(p)` | `RedisOption` | 密码 |
| `xOptCache.WithRedisDB(db)` | `RedisOption` | 数据库序号 |
| `xOptCache.WithRedisPoolSize(n)` | `RedisOption` | 连接池大小 |
| `xOptCache.WithRedisMinIdleConns(n)` | `RedisOption` | 最小空闲连接数 |
| `xOptCache.WithRedisDialTimeout(d)` | `RedisOption` | 连接建立超时 |
| `xOptCache.WithRedisReadTimeout(d)` | `RedisOption` | 读操作超时 |
| `xOptCache.WithRedisWriteTimeout(d)` | `RedisOption` | 写操作超时 |

### Memory 子选项

| 选项 | 类型 | 说明 |
|------|------|------|
| `xOptCache.WithMemoryDefaultTTL(d)` | `MemoryOption` | 默认过期时间 |
| `xOptCache.WithMemoryMaxEntries(n)` | `MemoryOption` | 最大条目数 |
| `xOptCache.WithMemoryShardCount(n)` | `MemoryOption` | 分片数（提升并发） |

## 数据库配置

<TypeTable
  type={{
    'WithDatabase': {
      description: '将子包 DatabaseOption 包裹为顶层 Option，供 Register 使用',
      type: 'func(opts ...xOptDatabase.DatabaseOption) Option',
      required: false,
    },
  }}
/>

### 驱动构造（子包 `xOptDatabase`）

<TypeTable
  type={{
    'MySQL': {
      description: '配置 MySQL，dsn 为标准 MySQL 连接串',
      type: 'func(dsn string, opts ...DatabaseOption) DatabaseOption',
      required: false,
    },
    'Postgres': {
      description: '配置 PostgreSQL，dsn 为 PG 连接串',
      type: 'func(dsn string, opts ...DatabaseOption) DatabaseOption',
      required: false,
    },
    'SQLite': {
      description: '配置 SQLite，dsn 为文件路径或 :memory:',
      type: 'func(dsn string, opts ...DatabaseOption) DatabaseOption',
      required: false,
    },
    'Oracle': {
      description: 'v1.0.4 新增。配置 Oracle，dsn 为 godror logfmt 格式连接串，依赖 Oracle Instant Client',
      type: 'func(dsn string, opts ...DatabaseOption) DatabaseOption',
      required: false,
    },
    'SQLServer': {
      description: 'v1.0.4 新增。配置 SQL Server，dsn 为 sqlserver:// URL 连接串',
      type: 'func(dsn string, opts ...DatabaseOption) DatabaseOption',
      required: false,
    },
    'FromEnv': {
      description: '从环境变量自动装配（读取 DATABASE_DRIVER 等），按驱动调用对应拼装函数，未启用时返回 nil',
      type: 'func(opts ...DatabaseOption) DatabaseOption',
      required: false,
    },
  }}
/>

### Oracle DSN 格式

`Oracle` 接受 godror logfmt 格式连接串：

```text
user="scott" password="tiger" connectString="dbhost:1521/orclpdb1" libDir="/path/to/instantclient"
```

> **平台差异**：Oracle 驱动底层为 godror（ODPI-C），依赖 Oracle Instant Client。
> - **macOS / Windows**：必须通过 `libDir` 字段或环境变量 `DATABASE_LIB_DIR` 指定 Instant Client 库目录。
> - **Linux**：留空 `libDir`，改用 `ldconfig` 将 Instant Client 加入系统库搜索路径。
>
> 如需从环境变量自动拼装，改用 `xOptDatabase.FromEnv()` 并设置 `DATABASE_DRIVER=oracle`。

### SQL Server DSN 格式

`SQLServer` 接受 URL 格式连接串：

```text
sqlserver://user:pass@host:port?database=dbname
```

### 通用二级选项

| 选项 | 类型 | 说明 |
|------|------|------|
| `xOptDatabase.WithMaxOpenConns(n)` | `DatabaseOption` | 最大打开连接数 |
| `xOptDatabase.WithMaxIdleConns(n)` | `DatabaseOption` | 最大空闲连接数 |
| `xOptDatabase.WithConnMaxLifetime(d)` | `DatabaseOption` | 连接最大存活时间 |
| `xOptDatabase.WithConnMaxIdleTime(d)` | `DatabaseOption` | 连接最大空闲时间 |
| `xOptDatabase.WithTablePrefix(p)` | `DatabaseOption` | 表名前缀（`FromEnv` 已自动读 `DATABASE_PREFIX`） |
| `xOptDatabase.WithAutoMigrate(tables...)` | `DatabaseOption` | AutoMigrate 目标表（可多次叠加） |
| `xOptDatabase.WithPrepare(fns...)` | `DatabaseOption` | 建表后数据初始化回调（按注册顺序执行） |

## 路由配置

<TypeTable
  type={{
    'WithRoute': {
      description: '注册路由函数，多次调用叠加执行',
      type: 'func(r RouteRegistrar) Option',
      required: false,
    },
    'WithRouteGroup': {
      description: '注册带前缀的路由组（自动调用 serve.Group）',
      type: 'func(prefix string, r func(rg *gin.RouterGroup)) Option',
      required: false,
    },
  }}
/>

```go
// RouteRegistrar 路由注册器，接收已装配依赖的 ctx（含 DB/缓存等组件）与 Gin 引擎进行路由挂载
type RouteRegistrar func(ctx context.Context, serve *gin.Engine)
```

## 使用示例

### 完整配置（Redis + MySQL + 路由）

```go title="main.go"
package main

import (
    "context"
    "time"

    xLog "github.com/bamboo-services/bamboo-base-go/common/log"
    xMain "github.com/bamboo-services/bamboo-base-go/major/main"
    xOption "github.com/bamboo-services/bamboo-base-go/major/option"
    xOptCache "github.com/bamboo-services/bamboo-base-go/major/option/cache"
    xOptDatabase "github.com/bamboo-services/bamboo-base-go/major/option/database"
    xReg "github.com/bamboo-services/bamboo-base-go/major/register"
)

func main() {
    // v1.0.5 起：opts 直接通过变参传给 Register
    reg := xReg.Register(context.Background(), nil)

    log := xLog.WithName(xLog.NamedMAIN)

    // 声明式配置：两层调用形态
    opts := []xOption.Option{
        // 缓存：Redis 后端
        xOption.WithCache(xOptCache.WithRedis("localhost:6379",
            xOptCache.WithRedisPassword("xxx"),
            xOptCache.WithRedisDB(0),
        )),
        // 数据库：MySQL
        xOption.WithDatabase(xOptDatabase.MySQL(
            "user:pass@tcp(localhost:3306)/bamboo?charset=utf8mb4&parseTime=True",
        )),
        // 路由
        xOption.WithRoute(func(ctx context.Context, serve *gin.Engine) {
            serve.GET("/ping", func(c *gin.Context) {
                c.String(200, "pong")
            })
        }),
    }

    // Runner 根据 opts 自动装配 Redis 客户端、GORM DB 等
    xMain.Runner(reg, log, opts)

    // 也支持传入后台协程函数
    // xMain.Runner(reg, log, opts, goroutineFunc)
}
```

### 仅缓存 + Memory 后端

```go
opts := []xOption.Option{
    xOption.WithCache(xOptCache.WithMemory(
        xOptCache.WithMemoryDefaultTTL(10*time.Minute),
        xOptCache.WithMemoryMaxEntries(10000),
    )),
    xOption.WithRoute(func(ctx context.Context, serve *gin.Engine) {
        serve.GET("/ping", func(c *gin.Context) {
            c.JSON(200, gin.H{"message": "pong"})
        })
    }),
}

xMain.Runner(reg, log, opts)
```

### Oracle 数据库

```go title="main.go"
opts := []xOption.Option{
    // Oracle：显式 DSN（macOS/Windows 必须包含 libDir）
    xOption.WithDatabase(xOptDatabase.Oracle(
        `user="scott" password="tiger" connectString="dbhost:1521/orclpdb1" libDir="/opt/oracle/instantclient"` ,
        xOptDatabase.WithMaxOpenConns(50),
    )),
    xOption.WithRoute(routeFunc),
}
xMain.Runner(reg, log, opts)
```

### SQL Server 数据库

```go title="main.go"
opts := []xOption.Option{
    // SQL Server：URL 格式连接串
    xOption.WithDatabase(xOptDatabase.SQLServer(
        "sqlserver://sa:Pa55w0rd@localhost:1433?database=mydb",
    )),
    xOption.WithRoute(routeFunc),
}
xMain.Runner(reg, log, opts)
```

### 从环境变量装配（缓存 + 数据库）

```go
opts := []xOption.Option{
    // 读取 NOSQL_DRIVER 等，未设置时返回 nil（被 WithCache 跳过）
    xOption.WithCache(xOptCache.FromEnv()),
    // 读取 DATABASE_DRIVER/DATABASE_HOST/... 等，按驱动自动选择拼装函数
    // 支持 mysql / postgres / sqlite / oracle / sqlserver
    xOption.WithDatabase(
        xOptDatabase.FromEnv(),
        xOptDatabase.WithAutoMigrate(&entity.Role{}, &entity.User{}),
        xOptDatabase.WithPrepare(seedRoles),
    ),
    xOption.WithRoute(routeFunc),
}

xMain.Runner(reg, log, opts)
```

### 条件构造

nil Option 与 nil 子包二级选项都会被自动跳过，可安全用于条件分支：

```go
var opts []xOption.Option

if useRedis {
    // cond && WithCache(...) 短路求值：false 时整体为 nil，被 Apply 跳过
    opts = append(opts, useRedis && xOption.WithCache(xOptCache.WithRedis("localhost:6379")))
} else {
    opts = append(opts, xOption.WithCache(xOptCache.WithMemory()))
}

opts = append(opts, xOption.WithRoute(routeFunc))

xMain.Runner(reg, log, opts)
```

## 与注册节点模式的关系

xOption 是注册节点模式的**补充**，而非替代。两者可以共存：

| 场景 | 推荐方式 |
|------|----------|
| 框架内置组件（缓存、数据库、路由） | xOption |
| 自定义初始化逻辑（第三方 SDK、业务启动） | xRegNode |
| 需要访问前序节点结果 | xRegNode（节点化注入） |

```go
// 混合模式：内置组件用 Option，自定义逻辑用节点
reg := xReg.Register(ctx,
    []xRegNode.RegNodeList{
        {Key: xCtx.CustomClientKey, Node: initCustomSDK},
    },
    // v1.0.5 起：opts 可直接作为变参传给 Register
    xOption.WithCache(xOptCache.WithRedis("localhost:6379")),
    xOption.WithRoute(routeFunc),
)

xMain.Runner(reg, log, nil)
```

## 下一步

<Cards>
  <Card title="缓存系统" href="/docs/bamboo-base-go/components/cache" />
  <Card title="服务运行时" href="/docs/bamboo-base-go/core/init/runner" />
  <Card title="节点初始化" href="/docs/bamboo-base-go/core/init" />
</Cards>
