---
title: 核心说明
description: Bamboo Base 整体架构设计与模块说明
icon: Layers
---

import { TypeTable } from '@/components/type-table';

# 核心架构

## 项目概述

**模块名称**: `github.com/bamboo-services/bamboo-base-go`

**Go 版本**: 1.25+

**项目定位**: Go 语言基础框架库，为 Bamboo 服务提供核心功能支持，包括日志、错误处理、雪花算法 ID 生成、验证器、中间件等基础设施。

## 包结构说明

```
github.com/bamboo-services/bamboo-base-go/
│
├── defined/                    # 🏷️ 定义层 — 零依赖的常量与类型
│   ├── context/                #   上下文键常量 (xCtx)
│   └── env/                    #   环境变量常量 (xEnv)
│
├── common/                     # 🧱 公共层 — 可独立使用的基础设施
│   ├── base_response.go        #   统一响应结构体 (xBase)
│   ├── error/                  #   错误码和错误处理 (xError)
│   ├── log/                    #   日志系统 (xLog)
│   ├── snowflake/              #   雪花算法 ID 生成 (xSnowflake)
│   ├── utility/                #   工具函数 (xUtil)
│   │   └── context/            #   上下文工具 (xCtxUtil)
│   └── validator/              #   验证器 (xVaild)
│
├── major/                      # 🚀 主框架层 — 依赖 Gin 的核心组件
│   ├── cache/                  #   泛型缓存系统 (xCache) — Redis / Memory 双后端
│   │   ├── driver/             #     缓存接口定义与类型
│   │   ├── redis/              #     Redis 后端实现
│   │   └── memory/             #     Memory 后端实现
│   ├── helper/                 #   核心中间件 (xHelper)
│   ├── hook/                   #   钩子函数 (xHook)
│   ├── main/                   #   服务运行时 (xMain)
│   ├── middleware/             #   业务中间件 (xMiddle)
│   ├── models/                 #   数据模型基类 (xModels)
│   ├── option/                 #   声明式配置 (xOption)
│   │   └── database/           #     数据库选项配置
│   ├── register/               #   应用注册和初始化 (xReg)
│   │   ├── init/               #     内置初始化节点
│   │   └── node/               #     节点管理器
│   ├── result/                 #   响应结果处理 (xResult)
│   ├── route/                  #   路由处理 (xRoute)
│   └── utility/                #   请求绑定与上下文提取 (xUtil)
│
└── plugins/                    # 🔌 插件层 — 可选扩展
    ├── grpc/                   #   gRPC 支持
    │   ├── constant/           #     gRPC 常量
    │   ├── generate/           #     Proto 生成辅助
    │   ├── interceptor/        #     拦截器
    │   ├── middleware/         #     gRPC 中间件
    │   ├── result/             #     gRPC 响应处理
    │   └── runner/             #     gRPC 运行时
    ├── async/                  #   异步任务执行 (xAsync)
    ├── cron/                   #   定时任务 (xCron)
    │   └── runner/             #     Cron 运行时
    └── email/                  #   邮件服务 (xEmail)
```

## 包导入别名

### defined 层（零依赖）

<TypeTable
  type={{
    'xCtx': {
      description: '上下文键常量定义',
      type: '.../defined/context',
      required: true,
    },
    'xEnv': {
      description: '环境变量管理，类型安全的读取函数',
      type: '.../defined/env',
      required: true,
    },
  }}
/>

### common 层（基础设施）

<TypeTable
  type={{
    'xBase': {
      description: '公共层根包，统一响应结构体 BaseResponse',
      type: '.../common',
      required: true,
    },
    'xError': {
      description: '错误码定义和错误处理',
      type: '.../common/error',
      required: true,
    },
    'xLog': {
      description: '日志系统，支持彩色控制台和 JSON 文件输出',
      type: '.../common/log',
      required: true,
    },
    'xSnowflake': {
      description: '雪花算法 ID 生成，支持业务基因',
      type: '.../common/snowflake',
      required: true,
    },
    'xUtil': {
      description: '通用工具函数',
      type: '.../common/utility',
      required: true,
    },
    'xCtxUtil': {
      description: '上下文工具函数',
      type: '.../common/utility/context',
      required: true,
    },
    'xVaild': {
      description: '验证器扩展，自定义规则和中文翻译',
      type: '.../common/validator',
      required: true,
    },
  }}
/>

### major 层（主框架）

<TypeTable
  type={{
    'xReg': {
      description: '应用注册和初始化',
      type: '.../major/register',
      required: true,
    },
    'xMain': {
      description: '服务运行时（统一启动、信号监听、优雅关闭）',
      type: '.../major/main',
      required: true,
    },
    'xOption': {
      description: '声明式配置选项（缓存、数据库、路由等）',
      type: '.../major/option',
      required: false,
    },
    'xHelper': {
      description: '核心中间件（请求上下文、Panic 恢复、HTTP 日志）',
      type: '.../major/helper',
      required: true,
    },
    'xMiddle': {
      description: '业务中间件（CORS、响应处理等）',
      type: '.../major/middleware',
      required: true,
    },
    'xResult': {
      description: '响应结果处理函数',
      type: '.../major/result',
      required: true,
    },
    'xRoute': {
      description: '路由处理（404、405）',
      type: '.../major/route',
      required: true,
    },
    'xModels': {
      description: 'GORM 实体基类，自动生成雪花 ID',
      type: '.../major/models',
      required: true,
    },
    'xCache': {
      description: '泛型缓存系统，Redis / Memory 双后端',
      type: '.../major/cache',
      required: false,
    },
    'xHook': {
      description: 'Redis 钩子，自动添加键前缀',
      type: '.../major/hook',
      required: false,
    },
    'xUtil': {
      description: '请求参数绑定与上下文组件提取',
      type: '.../major/utility',
      required: true,
    },
  }}
/>

### plugins 层（可选扩展）

<TypeTable
  type={{
    'xGrpcRunner': {
      description: 'gRPC 运行时',
      type: '.../plugins/grpc/runner',
      required: false,
    },
    'xGrpcInterface': {
      description: 'gRPC 拦截器',
      type: '.../plugins/grpc/interceptor',
      required: false,
    },
    'xAsync': {
      description: '异步任务执行器，支持链路追踪',
      type: '.../plugins/async',
      required: false,
    },
    'xCronRunner': {
      description: '定时任务运行时',
      type: '.../plugins/cron/runner',
      required: false,
    },
    'xEmail': {
      description: '邮件发送客户端',
      type: '.../plugins/email',
      required: false,
    },
  }}
/>

## 模块依赖关系

<Mermaid
  chart="
graph TD
    subgraph defined[defined 层]
        xCtx[xCtx<br/>上下文常量]
        xEnv[xEnv<br/>环境变量]
    end

    subgraph common[common 层]
        xBase[xBase<br/>BaseResponse]
        xError[xError<br/>错误处理]
        xLog[xLog<br/>日志系统]
        xSnowflake[xSnowflake<br/>雪花算法]
        xCtxUtil[xCtxUtil<br/>上下文工具]
        xVaild[xVaild<br/>验证器]
    end

    subgraph major[major 层]
        xReg[xReg<br/>注册中心]
        xOption[xOption<br/>声明式配置]
        xResult[xResult<br/>响应处理]
        xModels[xModels<br/>数据模型]
        xCache[xCache<br/>缓存系统]
        xHelper[xHelper<br/>核心中间件]
        xMain[xMain<br/>运行时]
        xUtil[xUtil<br/>请求绑定]
    end

    subgraph plugins[plugins 层]
        xGrpc[xGrpc<br/>gRPC 扩展]
        xAsync[xAsync<br/>异步任务]
        xCron[xCron<br/>定时任务]
        xEmail[xEmail<br/>邮件服务]
    end

    xBase --> xError
    xCtxUtil --> xCtx
    xCtxUtil --> xSnowflake

    xReg --> xLog
    xReg --> xSnowflake
    xReg --> xHelper
    xReg --> xVaild
    xOption --> xCache
    xOption --> xMain
    xResult --> xBase
    xResult --> xError
    xModels --> xSnowflake
    xCache --> xLog
    xMain --> xReg
    xMain --> xOption
    xMain --> xModels

    xGrpc --> xLog
    xGrpc --> xError
    xAsync --> xLog
    xAsync --> xCtxUtil
    xCron --> xLog
    xCron --> xSnowflake
    xEmail --> xLog"
/>

## 初始化流程

<Mermaid
  chart="
flowchart TD
    A[Register] --> B[ConfigInit<br/>加载 .env 配置]
    B --> C[LoggerInit<br/>初始化日志系统]
    C --> D[SnowflakeInit<br/>初始化雪花算法节点]
    D --> E[EngineInit<br/>创建 Gin 引擎]
    E --> F[injectContext<br/>合并初始化上下文]
    F --> G[返回 *Reg]"
/>

```go title="main.go"
package main

import (
    "context"

    xReg "github.com/bamboo-services/bamboo-base-go/major/register"
)

func main() {
    // [!code highlight:2]
    // 一键初始化所有组件
    reg := xReg.Register(context.Background(), nil)

    // 注册路由...

    // [!code highlight]
    reg.Serve.Run(":8080")
}
```

## 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `gin-gonic/gin` | v1.12+ | Web 框架 |
| `go-playground/validator` | v10.30+ | 验证器 |
| `joho/godotenv` | v1.5+ | 环境变量加载 |
| `redis/go-redis` | v9.21+ | Redis 客户端 |
| `gorm.io/gorm` | v1.31+ | ORM 框架 |
| `wneessen/go-mail` | v0.6+ | 邮件发送 |
| `robfig/cron/v3` | v3.0+ | 定时任务 |
| `google.golang.org/grpc` | v1.82+ | gRPC 框架 |

## 设计原则

1. **分层解耦** - 四层架构（defined → common → major → plugins），按需引入
2. **单一职责** - 每个包只负责特定的功能领域
3. **统一命名** - 所有包使用 `x` 前缀别名，便于识别
4. **约定优于配置** - 提供合理的默认值，减少配置负担
5. **链路追踪** - 请求 ID 贯穿整个请求生命周期
6. **后端可切换** - 缓存等组件支持 Redis / Memory 双后端，零代码切换

## 下一步

<Cards>
  <Card title="快速开始" href="/docs/bamboo-base-go/core/init" />
  <Card title="响应处理" href="/docs/bamboo-base-go/core/result/result" />
</Cards>
